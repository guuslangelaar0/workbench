# {{PROJECT_NAME}} — Session State

_The handoff. The next session must be able to resume from this file alone. Update it on cadence (and `/workbench:checkpoint` writes it for you). Keep the top section current; append dated entries below._

## Now (current snapshot)

- **Current focus:** (what is actively being worked)
- **Last commit per repo:** (sha — repo)
- **Build status:** (compiles? tests? per repo)
- **Blockers / decisions awaiting:** (or "none")
- **Next action:** (the single next thing the resuming session should do)

## Deployment / prod gap (if any)

(what is undeployed; what is staged; what remains)

## Log (dated, newest first)

<!-- append: YYYY-MM-DD HH:MM — what changed, what was verified, what failed -->
