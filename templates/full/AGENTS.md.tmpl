# AGENTS.md — {{PROJECT_NAME}}

This file grounds any coding agent (Claude Code, Codex, or other) working in this repo. The single source of truth for how we work is **`CLAUDE.md`** (project rules, the loop, honesty bar) plus:

- `.claude/SOUL.md` — the lead engineer's operating charter.
- `.claude/tasks/README.md` — the file-based task lifecycle and format.
- `.claude/SESSION_STATE.md` — current status, blockers, what the next session should do.
- `.claude/CODEX_COORDINATION.md` — if present, the shared Claude↔Codex operating agreement.

Before non-trivial work: read those, check `git status`, and pick up in-development tasks before starting new ones. Disk is the source of truth — trust it over chat summaries. Keep commits scoped (`git commit -- <paths>`) when other sessions may be active.
