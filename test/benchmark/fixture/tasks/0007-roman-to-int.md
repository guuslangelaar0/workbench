# 0007 — roman_to_int

**Status:** backlog
**Verification:** source the file and exercise the function on the spec's cases (incl. edge cases)

## Why
In `src/0007.sh`, define `roman_to_int s` that parses a valid Roman numeral string (1..3999) and prints the integer, handling the subtractive cases. E.g. IV -> 4, IX -> 9, LVIII -> 58, XC -> 90, CD -> 400, MCMXCIV -> 1994, MMMCMXCIX -> 3999. This is the inverse of task 0005.

## Acceptance criteria
- [ ] the function exists in the named file and is correct for all specified cases, including the subtractive pairs (IV, IX, XL, XC, CD, CM)

## Verification evidence
(populated when verified — the commands run + their output)
