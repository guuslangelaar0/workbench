# 0003 — reverse

**Status:** backlog
**Verification:** the artifact file matches the spec exactly

## Why
Write the reverse of the string abc to artifacts/rev.txt

## Acceptance criteria
- [ ] the artifact file exists with exactly the specified content

## Verification evidence
(populated when verified — the artifact content + how it was checked)
